@model IEnumerable<Riaad_EventEase.Models.AppEvent>

@{
    ViewData["Title"] = "Events Without Bookings";
}

<h2>Events Without Bookings</h2>

<ul>
    @foreach (var evt in Model)
    {
        <li>@evt.EventName (@evt.EventDate.ToShortDateString())</li>
    }
</ul>
